// Import Actions
import {  } from './AttendanceActions';

// Initial State
const initialState = {};

const AttendanceReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AttendanceReducer;
