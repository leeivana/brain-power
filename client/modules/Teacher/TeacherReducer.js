// Import Actions
import {  } from './TeacherActions';

// Initial State
const initialState = {};

const TeacherReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default TeacherReducer;
