// Import Actions
import {} from './StudentActions';

// Initial State
const initialState = {};

const StudentReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default StudentReducer;
