// Import Actions
import {  } from './AttendActions';

// Initial State
const initialState = {};

const AttendReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AttendReducer;
