// Import Actions
import {  } from './ClassActions';

// Initial State
const initialState = {};

const ClassReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ClassReducer;
