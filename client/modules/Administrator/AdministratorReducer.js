// Import Actions
import {  } from './AdministratorActions';

// Initial State
const initialState = {};

const AdministratorReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AdministratorReducer;
